#include <stdio.h>
/**
 * main - entry point. prints number of arguments passed
 * @argc: int. argument count
 * @argv: int. argument array
 * Return: 0 (success)
 */
int main(int argc, char *argv[])
{
	argv = argv;

	{
		printf("%d\n", argc - 1);
	}
	return (0);
}
