#include "main.h"

/**
 * infinite_add - adds two numbers
 * @n1: first number
 * @n2: second number
 * @r: buffer address
 * @size_r: size of address
 * Return: address (success), 0 (fail)
 */
char *infinite_add(char *n1, char *n2, char *r, int size_r)
{

    return (r);
}
